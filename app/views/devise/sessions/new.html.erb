<h2 class="text-center mb-4">Logowanie</h2>

<div class="d-flex justify-content-center">
  <div class="card" style="width: 30rem;">
    <div class="card-body">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "form-horizontal" }) do |f| %>
        <div class="form-group mb-3">
          <%= f.label :email, class: "control-label" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>

        <div class="form-group mb-3">
          <%= f.label :hasło, class: "control-label" %>
          <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="form-group form-check mb-3">
            <%= f.check_box :remember_me, class: "form-check-input" %>
            <%= f.label :zapamiętaj_mnie, class: "form-check-label" %>
          </div>
        <% end %>

        <div class="form-group text-center">
          <%= f.submit "Zaloguj się", class: "btn btn-primary", data: { turbo: false } %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>



<%#= link_to "Wyloguj się", destroy_user_session_path, 'data-turbo-method': :delete %>